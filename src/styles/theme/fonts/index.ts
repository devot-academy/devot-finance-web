import { FontFormat } from './interfaces'

export const FONTS: FontFormat = {
  fonts: {
    body: "'Roboto', sans-serif",
    heading: "'Mina', sans-serif"
  },
  fontWeights: {
    regular: 400,
    medium: 500,
    bold: 700
  }
}
